# UI / UX Rules

- Clean and simple layout
- Desktop-first, responsive is a plus
- Disable buttons while loading
- Show clear error messages
- Do not auto-save any data
- Only save on explicit user action

Navigation:
Login → Config → Chart
Register → Login