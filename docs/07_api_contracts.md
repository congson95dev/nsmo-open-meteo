# API Usage Rules

- All APIs are HTTP-based
- Assume JSON format
- Handle loading & error states
- Never assume API always returns data

Frontend must:
- Be defensive with null / empty responses
- Gracefully fallback to default values